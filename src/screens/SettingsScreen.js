import React from 'react';
import {Button, StyleSheet, View, Text} from 'react-native';
// const { PrismModule } = NativeModules;

const Settings = (props) => {
    console.log(`Entering Settings screen`);
    return (
        <View>
            <Text>Settings</Text>
        </View>
    );
};

export default Settings